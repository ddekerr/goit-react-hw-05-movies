import styled from '@emotion/styled';

export const ReviwsList = styled.ul`
  margin: 0;
  padding: 0;
  list-style: none;
`;

export const Review = styled.li`
  :not(:last-of-type) {
    margin-bottom: 10px;
  }
`;
